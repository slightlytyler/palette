// Text innder shadow
$text-inner-shadow
  &:before, &:after
    content: attr(title);
    color: rgba(255,255,255,.1);
    position: absolute;

  &:before 
    top: 1px
    left: 1px

  &:after
    top: 2px 
    left: 2px